import type { RequestContext } from '../api';
export interface HeadlessConfig {
    wpUrl: string;
    gqlUrl?: string;
    apiUrl?: string;
    blogUrlPrefix?: string;
    apiEndpoint?: string;
    apiClientSecret?: string;
    applyRequestContext?(url: string, init: RequestInit): Promise<RequestContext> | RequestContext;
}
export declare function normalizeConfig(config: HeadlessConfig): HeadlessConfig;
export declare function headlessConfig(config?: HeadlessConfig): HeadlessConfig;
export declare function getGqlUrl(): string;
