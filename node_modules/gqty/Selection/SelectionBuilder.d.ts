import type { InnerClientState } from '../Client/client';
import { Selection } from './selection';
export declare type BuildSelectionValue = (string | number) | [string | number, {
    args?: Record<string, unknown>;
    unions?: string[];
}?];
export declare type BuildSelectionInput = [
    'query' | 'mutation' | 'subscription',
    ...BuildSelectionValue[]
];
export interface BuildSelection {
    (type: 'query' | 'mutation' | 'subscription', ...values: BuildSelectionValue[]): Selection;
}
export declare function createSelectionBuilder(innerState: InnerClientState): BuildSelection;
