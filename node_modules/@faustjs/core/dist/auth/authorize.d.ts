import { CookieOptions } from './cookie';
export declare function authorize(code: string): Promise<{
    access_token?: string;
}>;
export declare function ensureAuthorization(redirectUri: string, options?: CookieOptions): string | {
    redirect: string;
} | undefined;
